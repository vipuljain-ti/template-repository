schema {
  query: Query
  mutation: Mutation
}

type Query {
  user(id: Int!): User
}

type Mutation {
  createUser(input: NewUserInput!): User
}

type User {
  id: Int!
  name: String!
  email: String!
}

input NewUserInput {
  name: String!
  email: String!
}
